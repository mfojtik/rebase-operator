apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: rebase-operator
  name: cache
spec:
  accessModes:
  - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 8Gi
